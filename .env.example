# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Admin CMS (required — set a strong password)
ADMIN_PASSWORD=change-me-to-a-strong-password
NEXT_PUBLIC_APP_NAME=Yoel Sitorus

# API
NEXT_PUBLIC_API_URL=http://localhost:3001/api

# Database (Prisma / PostgreSQL)
DATABASE_URL=postgresql://postgres:***@localhost:5432/yoel-portfolio

# Firebase Admin SDK (server-side) — legacy, migrating to Prisma
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=

# Firebase (client-side, for live Firestore bridge)
NEXT_PUBLIC_FIREBASE_API_KEY=***
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=***
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# Sentry (https://sentry.io)
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_ORG=zemetia
SENTRY_PROJECT=yoel-portfolio
SENTRY_AUTH_TOKEN=***

# PostHog (https://posthog.com)
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# MinIO (image storage)
# Endpoint is the internal MinIO server address (not the public URL)
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_USE_SSL=false
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=ej24zg9rhltre77t
MINIO_BUCKET=portfolio-images
# Public URL (served via CDN / reverse-proxy like nginx)
MINIO_PUBLIC_URL=https://api.storage.zemetia.com
